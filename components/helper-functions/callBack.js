const callBack = (cb) => {
	// easy callback function i can execute to create a callback within a react component
	// Call a anonymous function with a return value with the html
	return cb();
};

export default callBack;
